# Caption patterns

Use these as structures, not fill-in-the-blank permission to invent facts. Every
placeholder must be resolved from the clip map or a verified event source before the
caption enters the publishing queue.

## Rally

> Three touches, no wasted movement. `{event}` · `{round}`. Full gallery at
> letspepper.com/gallery. `{credit}`.

## Specific play

> `{play_detail}`. `{event}` · `{team_a}` vs `{team_b}`. `{credit}`.

## Final point

> Final point. `{winner}` closes `{round}`, `{score}`. `{event}`. `{credit}`.

## Photo

> One frame from `{event}`. Find your team at letspepper.com/gallery. `{credit}`.

## Schedule

> The bracket is set. First matches: `{time}`. Courts and matchups at `{url}`.

## Weather

> Weather update: `{status}`. Next update at `{time}`. Details at `{url}`.

Do not publish unresolved braces. Use zero to two relevant hashtags only when they aid
discovery; do not append a generic hashtag wall. Creator credit is factual metadata,
not decorative copy.
